comment "rtl88x2bu needs a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL

config BR2_PACKAGE_RTL88X2BU
	bool "rtl88x2bu"
	depends on BR2_LINUX_KERNEL
	help
	  rtl88x2bu wifi driver

	  https://github.com/cilynx/rtl88x2bu
