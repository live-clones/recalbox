; ===========================
; startrekvoyager
; ===========================

; all keys can be overridden for a particular board using key.<boardtag>
; example:
;   priority = 0
;   priority.rpi4 = 3
; Complete sections may be overridden. In such cases, all key from the global section are ignored,
; you must redefine them all
; example
; [core.0]
; [core.0.rpi4]
; Board tags are defined by BR2_PACKAGE_RECALBOX_TARGET_<tag>

[system]
name = "startrekvoyager"
fullname = "Star Trek Voyager"
platforms = "startrekvoyager"
theme.folder = "quake 1"
roms.folder = "%ROOT%/ports/Star Trek Voyager"
screenscraper.id = 
doc.link.fr = ""

[properties]
; Following properties can take the following values: arcade, console, handheld, computer, virtual, engine, port
type = engine
; Following properties can take the following values: no, optional, recommended, mandatory
device.pad = mandatory
device.keyboard = no
device.mouse = no
device.lightgun = no

[core.0]
package = BR2_PACKAGE_LIBRETRO_VITAVOYAGER
priority = 1
emulator = "libretro"
core = "vitavoyager"
extensions = ".pak .zip .7z"
netplay = 0
compatibility = high
speed = high
