diff --git a/linux/Config.in b/linux/Config.in
index de9bf0ad7a..bda41bb71c 100644
--- a/linux/Config.in
+++ b/linux/Config.in
@@ -436,6 +436,20 @@ config BR2_LINUX_KERNEL_DTB_OVERLAY_SUPPORT
 
 endif
 
+config BR2_LINUX_KERNEL_DTS_INSTALL_OVERLAYS
+	bool "Build and install Device Tree Overlays (dtbo)"
+	depends on BR2_LINUX_KERNEL_DTS_SUPPORT
+	help
+	  Build in-tree device tree overlays.
+	  Currently supports Raspberry Pi kernels.
+
+config BR2_LINUX_KERNEL_ADDITIONAL_IMAGES
+	string "Addtional images to build (like rk3566.img)"
+	help
+	  This is used to create resource.img with custom boot logo
+	  and kernel device tree blob.
+	  (do not set .img suffix)
+
 config BR2_LINUX_KERNEL_INSTALL_TARGET
 	bool "Install kernel image to /boot in target"
 	depends on !BR2_TARGET_ROOTFS_INITRAMFS
